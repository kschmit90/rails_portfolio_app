<h1>Contacts Index</h1>

<% @contacts.each do |contact| %>
  <p><%= link_to(contact.first_name + " " + contact.last_name, contacts_show_path(contact.id)) %></p>
<% end %>

<%= link_to("New Contact", new_contact_path) %>