<h1>Edit Link</h1>

<% @link.errors.full_messages.each do |msg| %>
  <p><%= msg %></p>
<% end %>

<%= form_for(@link, :url => update_link_path) do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>
	
	<%= f.label :url %>
	<%= f.text_field :url %>
	
	<%= f.submit %>
	
<% end %>